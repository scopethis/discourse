describe('PromptList trait', function() {
  it('must support adding a prompt')
  it('must support removing a prompt')
  it('should show the number of prompts')
  it('must support prompt re-ordering')
})